<%= simple_form_for [family, memory],
  html: {class: "d-flex"} do |f|%>
  <%= f.input :text %>
  <%= f.association :pet, collection: family.pets %>
  <%= f.submit "Send", class: "btn btn-primary mb-3" %>
<% end %>
